# Copy this file to .env.local (for `vercel dev`) and fill in real values.
# Never commit .env.local — it's already in .gitignore.

# Supabase → Project Settings → API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Same project, public-safe keys for the browser client (used for login/signup)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Omise → Dashboard → Keys (use TEST keys for development)
OMISE_SECRET_KEY=
VITE_OMISE_PUBLIC_KEY=
